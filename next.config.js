/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  basePath: '/jackieng-profile',
  images: { unoptimized: true },
  trailingSlash: true,
}

module.exports = nextConfig
